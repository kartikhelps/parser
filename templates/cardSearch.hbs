import * as React from "react";
import { styled } from "@mui/material/styles";
import Grid from "@mui/material/Grid";
import Paper from "@mui/material/Paper";
import Typography from "@mui/material/Typography";
import ButtonBase from "@mui/material/ButtonBase";
import Complex from "./complex.jpg";
import StairsIcon from "@mui/icons-material/Stairs";
import HomeIcon from "@mui/icons-material/Home";
import CropDinIcon from "@mui/icons-material/CropDin";
import FavoriteBorderIcon from "@mui/icons-material/FavoriteBorder";
import { Button, Divider, Rating } from "@mui/material";

const Img = styled("img")({
  margin: "auto",
  display: "block",
  maxWidth: "100%",
  maxHeight: "100%",
});

const StyledRating = styled(Rating)({
  fontSize: "2.5rem", // Adjust the size as per your preference
});

export default function ComplexGrid() {
  return (
    <Paper
      sx={{
        p: 2,
        margin: "auto",
        maxWidth: "80%",
        flexGrow: 1,
      }}
    >
      <Grid container alignItems="center">
        <Grid item xs={12} sm={6}>
          <Typography variant="h6">Fullbrix</Typography>
          <Typography variant="h6">DLF PHASE 3 GURUGRAM, Haryana</Typography>
        </Grid>
        <Grid item xs={12} sm={6} textAlign="right">
          <FavoriteBorderIcon />
        </Grid>
      </Grid>
      <Divider sx={{ my: 2 }} />
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Grid container spacing={2}>
            <Grid item>
              <Img alt="complex" src={Complex} />
            </Grid>
            <Grid item xs container direction="column" spacing={1}>
              <Grid item xs>
                <Typography gutterBottom variant="h5" component="div">
                  Flat 4 BHK
                </Typography>
                <Typography variant="subtitle1" gutterBottom>
                  4 BHK Builder Floor in Gurgaon
                </Typography>
                <Typography variant="body2">
                  <Grid container spacing={1} alignItems="center">
                    <Grid item>
                      <HomeIcon fontSize="small" />
                    </Grid>
                    <Grid item>4 bhk</Grid>
                    <Grid item>
                      <StairsIcon fontSize="small" />
                    </Grid>
                    <Grid item>1st Floor</Grid>
                    <Grid item>
                      <CropDinIcon fontSize="small" />
                    </Grid>
                    <Grid item>500 sq yard</Grid>
                  </Grid>
                </Typography>
              </Grid>
              <Grid item>
                <Typography variant="h6" color="text.secondary">
                  <StyledRating name="no-value" value={4} />
                </Typography>
              </Grid>
              <Grid item container justifyContent="flex-end">
                <Button variant="contained">4.5 Cr</Button>
              </Grid>
            </Grid>
          </Grid>
        </Grid>
      </Grid>
    </Paper>
  );
}
